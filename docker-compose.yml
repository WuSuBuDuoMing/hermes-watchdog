version: "3.8"

services:
  hermes-watchdog:
    build: .
    container_name: hermes-watchdog
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
